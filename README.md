# A web Molecule Viewer

# Functions
## draw 2D to 3D
The web site can draw simple molecules and convert it to 3D structure

## view different 3D molecular format 
Supports gromacs `tpr`, `gro`, `chg` (from Multiwfn), `xyz`, `mol`, standard `pdb`

